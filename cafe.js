// Generated by CoffeeScript 1.7.1
(function() {
  define(['date_helper', 'url_helper', 'asset_tag_helper', 'form_options_helper', 'form_tag_helper'], function(DateHelper, UrlHelper, AssetTagHelper, FormOptionsHelper, FormTagHelper) {
    var k, root, v;
    root = typeof window !== "undefined" && window !== null ? window : global;
    for (k in DateHelper) {
      v = DateHelper[k];
      root[k] = v;
    }
    for (k in UrlHelper) {
      v = UrlHelper[k];
      root[k] = v;
    }
    for (k in AssetTagHelper) {
      v = AssetTagHelper[k];
      root[k] = v;
    }
    for (k in FormOptionsHelper) {
      v = FormOptionsHelper[k];
      root[k] = v;
    }
    for (k in FormTagHelper) {
      v = FormTagHelper[k];
      root[k] = v;
    }
    return root;
  });

}).call(this);
